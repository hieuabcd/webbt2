version: "3.9"

services:
  web:
    image: nginx:latest

    restart: always

    ports:
      - "3003:80"

    volumes:
      - ./:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf