version: "3.9"

services:
  web:
    image: nginx:latest
    restart: always

    ports:
      - "${APP_PORT}:80"

    volumes:
      - /opt/app/html:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf