version: "3.9"

services:
  web:
    image: nginx:latest
    restart: always

    container_name: ${APP_NAME}_web

    volumes:
      - ./:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf